/* =====================================================================
   E.B FIT — הגדרות חיבור לשרת
   ---------------------------------------------------------------------
   כל עוד שני השדות ריקים — האפליקציה עובדת בדיוק כמו קודם:
   הכל מקומי על המכשיר, בלי שרת ובלי התחברות.

   כדי להפעיל סנכרון, למלא מ-Supabase:
       Project Settings  ->  API
         Project URL   ->  URL
         anon public   ->  ANON

   ⚠️  אך ורק את מפתח ה-anon. את service_role לעולם לא שמים כאן —
       הוא עוקף את כל הגנות ה-RLS והקובץ הזה ציבורי.
   ===================================================================== */

window.EBFIT_CONFIG = {
  // כתובת הבסיס בלבד — בלי /rest/v1/ בסוף
  URL:  'https://yidvzlarrcxeonwkuygo.supabase.co',
  // המפתח הפומבי (publishable). מוגן ע"י RLS — לא מפתח סודי.
  ANON: 'sb_publishable_xZee-93eyIuq8NAJ_TRQoQ_f99064j9'
};
